# 工作拷贝验收记录

2026-09-25

- TypeScript 类型检查通过。
- 原引擎 10 项测试通过。
- 新入口 Vite 构建通过，独立单 HTML 4,815,641 字节。
- 文书样片从74秒实际播放到127秒，自动停止，音源数量归零。
- 23个镜头段可直接访问；文书、驿站与江岸画面在无关跳转后恢复相同像素。
- 8次反复重建音轨始终只有1个声源，停止后为0。
- 两条18秒模拟插段实际播放到结束，可比较后回到正片127秒。
- 互动版直接跳到211秒不会误弹选择。
- 单HTML断网载入无HTTP请求，音频和图像准备成功；手机390×844保持16:9画面及字幕边界。

首轮4项浏览器验收通过；新增直接跳章回归时发现开发服务未收到WSL文件变动、仍返回旧函数。重启本任务4176服务并启用文件轮询后，最新代码的跳章与画面重建复测通过。打包文件直接来自最新源码，另行离线验证通过。

视觉上已检查御案和海面交叉剪辑、手机字幕；目前仍是制作工作拷贝。见README中的待制作镜头清单。此记录不表示完成最终混音、全部场景精修或旁白盲听选型。


## 2026-09-26 虎门场景补强

- 新增独立虎门珠江口绘景，1400×787 WebP 约 198 KB；正片单 HTML 为 5,087,684 字节。
- S03 加入按故事时间计算的缓慢推镜、池面水纹、远岸雾层漂移与近景芦苇轻摆。
- S03 补三次木器刮擦、六次疏落水滴拟音，声部轻置于旁白下。
- 回归用例增加 S03 不同时间画面变化、任意跳转后同时间像素复现断言。
- 类型检查、核心引擎测试、工作拷贝构建、单 HTML 打包通过。
- 新增 S03 专项断言后，完整 Playwright 浏览器套件 4/4 通过（2.4 分钟），含正片播放、画面确定性、分支回接、离线手机页面。
- 美术仍为单张绘景上的 2.5D 动作层，远景人物未拆层，也未制作正式现场拟音或最终混音。
